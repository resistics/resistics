from pathlib import Path

datapath = Path("E:/", "magnetotellurics", "code", "resisticsdata", "formats")
timePath = datapath / "timeData"
timeImages = Path("timeImages")
calPath = Path("calData")
calImages = Path("calImages")