Configuration
-------------

Processing is configured through flow definitions, parameter sets, and gather
criteria stored beneath a project's ``processing`` directory.

The resistics configuration files allow users to:

- Specify processing parameters
- Save processing sequences for later jobs
