from pathlib import Path

datapath = Path("E:/", "magnetotellurics", "code", "resisticsdata", "advanced")

preprocessPath = datapath / "preprocessProject"
preprocessImages = Path("preprocessImages")
preprocessDoc = Path("..", "..", "docs", "source", "_examples", "advanced", "preprocess")

remotePath = datapath / "remoteProject"
remoteImages = Path("remoteImages")
remoteDoc = Path("..", "..", "docs", "source", "_examples", "advanced", "remote")

intersitePath = datapath / "intersiteProject"
intersiteImages = Path("intersiteImages")
intersiteDoc = Path("..", "..", "docs", "source", "_examples", "advanced", "intersite")


